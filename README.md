# Random
A simple C++ project for learning and experimenting with the C++11 random library

## Quick Start
Clone the repository and navigate to the project directory:
```bash
git clone https://github.com/Oganechan/Random
cd ./Random
```
Run the compilation script:
```bash
bash fast_compilate.sh
```
After compilation, you can run any of the example programs
